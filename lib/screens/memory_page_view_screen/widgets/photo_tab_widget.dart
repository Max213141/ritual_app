import 'package:flutter/material.dart';

class PhotoTabWidget extends StatelessWidget {
  const PhotoTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Photo gallery goes here'));
  }
}
